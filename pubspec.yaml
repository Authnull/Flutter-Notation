name: flutter_notation
description: A new flutter plugin project.
version: 1.0.0
homepage: https://authnull.com

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    androidPackage: com.authnull.flutter_notation
    pluginClass: FlutterNotationPlugin
